# 箱线图（Box Plot）

## 适用场景
- 展示数据分布的五数概括（最小值、Q1、中位数、Q3、最大值）
- 比较多个组的分布差异

## 设计要点

1. **箱体宽度**：与组数量成反比（组越多，箱体越窄）
2. **异常值标记**：用 × 或 ◇ 符号标记，透明度 0.6
3. **颜色**：单组用 #4269d0，多组按 categorical 色板
4. **须线**：默认 1.5 IQR，线宽 1px
