# 直方图（Histogram）

## 适用场景
- 展示单个数值变量的分布形态
- 识别数据的偏度、峰度、多峰等特征

## 设计要点

1. **箱数（Bins）**：默认使用 Sturges 公式（k = ⌈log₂(n) + 1⌉），或 Freedman-Diaconis 规则
2. **颜色**：默认 #4269d0，半透明（opacity 0.8）
3. **边线**：每个箱体用白色细线（0.5px）分隔
4. **叠加分布曲线**：可选叠加核密度估计曲线（#ff725c, 1.5px）
